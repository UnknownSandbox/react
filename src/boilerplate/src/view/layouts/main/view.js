import React from 'react';

export default ({children}) => (
    <div>
        <div>
            <h4>Main Layout</h4><br/>
        </div>
        <div>
            {children}
        </div>
    </div>
);
